export const baseUrl = process.env.REACT_APP_URL || "http://localhost:5000";

/*   process.env.ENVIRONMENT !== "localhost"
    ? "whateverdomain"
    : "http://localhost:5000";
console.log(process.env);
console.log(baseUrl); */
